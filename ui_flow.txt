UI FLOW & STRUCTURE NOTES
=========================
Last updated: 2026-03-29

CURRENT STATE
-------------
Full exercise flow is working end-to-end:
  Home → Exercise Setup → Exercise → Result
Settings, Calibration, and test pages are functional.
Portrait-only orientation locked.


FULL FLOW
---------

1. HOME / DASHBOARD
   ✅ "Start Practice" button → opens ExerciseSetupSheet
   ✅ Shortcut buttons for accelerometer test and microphone test
   ☐  Quick summary of recent activity (needs persistence layer)

2. EXERCISE SETUP (bottom sheet)
   ✅ Mode selector: Normal / Zen / Incremental / Survival / Challenge
   ✅ Difficulty: Easy / Medium / Hard
   ✅ BPM: +/- buttons (per-mode defaults)
   ✅ Sensor: Accelerometer / Mic / Mixed (headphone warning SnackBar)
   ✅ Start button → navigates to ExercisePage with all params

3. EXERCISE PAGE
   ✅ Queue animation with evaluated quarter states
   ✅ Mode-specific info chips:
       Normal:      nothing extra (lives not shown — Normal has no lives mechanic)
       Zen:         measures completed counter
       Incremental: current BPM, consecutive misses
       Survival:    lives + streak counter
       Challenge:   measure progress (X/16)
   ✅ Stop button
   ✅ Navigates to ExerciseResultPage when exercise stops

4. RESULT SCREEN
   ✅ Per-mode result:
       Normal:      pass/fail + measures completed
       Zen:         total measures played
       Incremental: max BPM reached
       Survival:    measures survived, lives left
       Challenge:   perfect/ok/miss breakdown
   ✅ Back to Home button
   ☐  Retry button (same config — needs config passed through to result page)

5. BOTTOM NAV TABS
   ✅ Home:     main practice flow
   ✅ Settings: latency, metronome volume, accel/mic thresholds, default sensor
   ☐  Stats:    history and progress (needs persistence layer)
   ☐  Profile:  student info (future)

6. SETTINGS
   ✅ Audio latency compensation slider (0–300ms)
   ✅ Metronome volume slider
   ✅ Accelerometer hit threshold slider
   ✅ Microphone hit threshold slider (sent to native via MethodChannel)
   ✅ Default sensor selector
   ✅ Open Calibration button
   ✅ Reset to defaults

7. CALIBRATION PAGE
   ✅ Audio latency: tap-along calibration (count-in → 8 beats → avg delta → apply)
   ✅ Sensor threshold: accel/mic selector, multi-sample average, apply button


MISSING / TODO (priority order)
---------------------------------
1. ✅ Retry button on result screen (config stored in ExerciseResult, re-launches with same params)
2. ☐  Persistence layer for session history (Stats tab)
3. ☐  Stats screen: history, progress charts
4. ☐  Profile screen (future)
5. ☐  Headphone detection (AudioManager) to replace SnackBar warning
6. ☐  Move test pages under Settings tab instead of home screen shortcuts


NOTES
-----
- Survival/Challenge modes not fully battle-tested
- Debug print statements still in measure_controller.dart and exercise_controller.dart
- Strategy pattern refactor for ExerciseMode noted as TODO in exercise_controller.dart
- BPM is per-session (set in setup sheet), not global
